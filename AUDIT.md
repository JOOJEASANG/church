# Repository Audit

최종 검토 기준일: 2026-08-18

## 검토 범위

- 사용자 앱과 관리자 앱의 인증·권한 흐름
- Realtime Database 읽기·쓰기·쿼리·검증 규칙
- Cloud Storage 업로드·삭제·소유권 규칙
- Cloud Functions 권한 동기화, 회원 탈퇴, 신청 처리, 성경퀴즈 서버 채점
- 교회 일정 기간 등록·수정 및 사용자 캘린더 표시
- 성경퀴즈 개인 학습·힌트·해설·직접 출제·실시간 대회 흐름
- PWA 설치, 캐시, FCM 서비스워커
- 관리자 화면과 사용자 화면의 모바일·PC 사용성
- GitHub Actions 품질 검사와 Firebase 배포 흐름

## 보안·데이터 무결성 보완 상태

- 신규 회원은 승인 대기로 생성하고 관리자 승인 후 쓰기 기능을 허용합니다.
- 최초 관리자 등록은 지정 UID로 제한하고 관리자 전체 목록은 관리자만 조회합니다.
- 교역자 전용 기도제목은 관리자와 작성자 외에는 조회할 수 없습니다.
- 본인 신청 내역은 `userUid` 제한 쿼리로 재로그인 후에도 조회합니다.
- 재능나눔·행사·모임 신청은 Callable Function의 원자적 트랜잭션으로 정원, 마감, 중복 신청을 검사합니다.
- 좋아요·댓글·기도 참여·신청 인원은 서버에서 실제 레코드를 기준으로 보정합니다.
- 회원 탈퇴는 서버에서 관련 데이터와 인증 계정을 함께 정리합니다.
- 게시물 파일은 승인 회원 UID 경로와 소유자 메타데이터를 사용합니다.
- FCM은 500개 단위로 발송하고 만료 토큰을 제거합니다.
- PWA와 FCM 서비스워커를 통합하고 알림 이동 URL을 동일 출처로 제한했습니다.
- 전역 `fetch` 덮어쓰기와 로그아웃 후 실시간 리스너 잔존 문제를 제거했습니다.

## 2026-08-18 전체 재점검 및 개선

- Cloud Functions 런타임을 Node.js 20에서 Node.js 22로 올려 런타임 사용 중단 위험을 제거했습니다.
- 중복 실행되던 Firebase 배포 워크플로를 하나로 통합했습니다.
- Functions 변경 감지를 마지막 커밋 한 개가 아닌 push 전체 변경 범위로 계산하도록 수정했습니다.
- `main` 브랜치와 기존 운영 브랜치 모두 품질 검사·배포 워크플로가 인식하도록 정리했습니다.
- 성경퀴즈 메인 문구 보정 MutationObserver는 문구 적용 즉시 종료되고 최대 10초 뒤 강제 종료되도록 개선했습니다.
- 공통 UI 보정 모듈에서 키보드 포커스 표시, 모바일 확대 허용, 터치 반응, reduced-motion 설정 존중을 추가했습니다.
- 관리자 모바일 헤더의 권한·홈·로그아웃 아이콘 간격을 통일했습니다.
- PWA 캐시 버전을 갱신해 설치 앱에서도 새 화면 리소스가 자연스럽게 교체되도록 했습니다.

## 자동 검증

GitHub Actions에서 다음 검사를 수행합니다.

1. 반복 실행 가능한 저장소 하드닝 패치
2. 모든 JSON 파싱
3. 모든 JavaScript/MJS `node --check`
4. 핵심 인증·권한·개인정보 보호 패턴 검사
5. Cloud Functions 의존성 설치와 문법 검사
6. Firebase Emulator Suite를 통한 Realtime Database·Storage Rules 컴파일

## 운영 확인 항목

- 운영 계정으로 신규 가입 → 승인 → 신청 → 취소 → 탈퇴 흐름 확인
- 관리자 계정으로 회원 승인, 공지·주보·일정 등록 및 수정 확인
- 성경퀴즈 개인 학습, 힌트·해설, 직접 문제 출제, 대회 방 생성·참가 확인
- 앱 설치 후 아이콘, 서비스워커 갱신, 오프라인 화면, 푸시 알림 이동 확인
- 모바일에서 관리자 헤더 아이콘과 주요 폼의 터치·포커스 상태 확인

브라우저·실기기 통합 동작은 실제 Firebase 운영 프로젝트의 인증 제공자, 데이터, FCM 권한 및 배포 상태에 영향을 받으므로 운영 배포 후 계정과 실기기에서 최종 확인합니다.

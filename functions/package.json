{
  "name": "church-functions",
  "version": "2.2.0",
  "private": true,
  "description": "천안남산교회 PWA Cloud Functions",
  "main": "main.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "node --check main.js && node --check index.js && node --check bible-quiz.js && node --check bible-quiz-v2.js && node --check bible-quiz-data.js && node --check bible-quiz-expanded-data.js && node --check bible-quiz-taxonomy.js",
    "test": "node --test",
    "serve": "firebase emulators:start --only functions,database,storage",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.2"
  }
}
